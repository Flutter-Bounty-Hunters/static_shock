<!doctype html>

<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ title }} | Static Shock</title>

    {{ components.favicon() }}

    {{ components.bootstrapJs() }}

    {{ components.highlightJs() }}

    {{ components.fontAwesome() }}

    <link rel="stylesheet" href="/styles/theme.css">
    <link rel="stylesheet" href="/styles/base_layout.css">
    <link rel="stylesheet" href="/styles/inner_page.css">
    <link rel="stylesheet" href="/styles/guides.css">
  </head>

  <body class="inner-page">
    <main>
      {{ components.navbar({"title": title}) }}

      <div style="height: 200px;">&nbsp;</div>
      <div class="container">
        <div class="d-flex flex-nowrap" style="max-width: 1000px; margin: auto;">
          <nav class="guides-menu">
            {% for menuItem in docs_menu %}
              <a class="btn btn-primary btn-sm" href="/guides/{{ menuItem.id }}" role="button">{{ menuItem.title }}</a>
            {% endfor %}
          </nav>
          <section class="guide-content w-100">
            {{ content }}
          </section>
        </div>
      </div>
    </main>

    <footer>
      {{ components.footer() }}
    </footer>
  </body>
</html>